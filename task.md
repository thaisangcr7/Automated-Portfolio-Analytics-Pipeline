# Task List: Automated Portfolio Analytics Pipeline

- `[x]` Phase 1: Environment & Warehousing (Docker, Postgres, pgAdmin)
  - `[x]` Create `docker-compose.yml` defining PostgreSQL and pgAdmin services
  - `[x]` Test container startup and database connection via pgAdmin
- `[x]` Phase 2: Ingestion & Extraction (Python & Yahoo Finance)
  - `[x]` Create ingestion script (`scripts/extract_data.py`) to fetch stock prices (e.g., JPM, C, AAPL, MSFT)
  - `[x]` Write script loading logic to load raw JSON/CSV data into Postgres `raw` schema
  - `[x]` Verify raw data ingestion via pgAdmin
- `[/]` Phase 3: Orchestration (Apache Airflow)
  - `[/]` Extend `docker-compose.yml` with Airflow services (Webserver, Scheduler, Postgres backend)
  - `[ ]` Create Airflow DAG (`dags/portfolio_orchestrator.py`) to run the extraction script daily
  - `[ ]` Run and verify the pipeline execution end-to-end via the Airflow UI
- `[ ]` Phase 4: Analytics Engineering (dbt Core)
  - `[ ]` Set up the dbt project and define connection profile (`profiles.yml`)
  - `[ ]` Build dbt seed file for mock portfolio holdings (`portfolio_holdings.csv`)
  - `[ ]` Develop staging models (Bronze) for stock prices and holdings
  - `[ ]` Develop intermediate models (Silver) to compute 50-day and 200-day moving averages
  - `[ ]` Develop marts models (Gold) representing a star schema (`dim_companies`, `fct_portfolio_valuation`)
  - `[ ]` Add dbt tests (uniqueness, nullability) and run assertions
  - `[ ]` Integrate dbt into the Airflow DAG to trigger runs automatically
- `[ ]` Phase 5: Visualization (Metabase)
  - `[ ]` Add Metabase service to `docker-compose.yml`
  - `[ ]` Connect Metabase to Postgres `analytics` database schema
  - `[ ]` Build dashboard visualizations (Total Value Trend, Moving Average charts, Sector Allocation)
